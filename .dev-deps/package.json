{
  "private": true,
  "scripts": {
    "lint": "tslint --config ../tslint.json --project ../tsconfig.options.json",
    "tsc": "tsc --version && tsc -b ../tsconfig.json",
    "check-dts-config": "tsc --lib es2015 --allowJs --noEmit ../dts-config.js"
  },
  "devDependencies": {
    "tslint": "~5.20.1",
    "typescript": "~4.1.3"
  }
}
