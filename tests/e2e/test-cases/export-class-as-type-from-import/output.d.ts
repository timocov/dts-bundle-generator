import { ClassName, Enum, anotherValue, makeClassName, valueHere } from 'package-with-values';

export type * from "package-with-values";

export {
	type ClassName,
	type Enum,
	type anotherValue,
	type makeClassName,
	type valueHere,
};

export {};
