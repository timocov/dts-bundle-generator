import { TestCaseConfig } from '../test-case-config';

const config: TestCaseConfig = {
	generatorOptions: {
		allowedTypesLibraries: [],
		importedLibraries: ['node', 'fs'],
	},
};

export = config;
